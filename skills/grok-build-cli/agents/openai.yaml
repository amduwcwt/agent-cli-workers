interface:
  display_name: "Grok Build CLI"
  short_description: "Run, verify, and learn from bounded Grok workers"
  default_prompt: "Use $grok-build-cli to run this bounded Grok task, verify its capsule independently, then record a privacy-minimized controller outcome."
