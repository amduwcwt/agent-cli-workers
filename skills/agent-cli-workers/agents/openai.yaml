interface:
  display_name: "Agent CLI Workers"
  short_description: "Run, verify, and learn from resumable CLI workers"
  default_prompt: "Use $agent-cli-workers to route one worker, collect and independently verify its capsule, then record a privacy-minimized controller outcome."
